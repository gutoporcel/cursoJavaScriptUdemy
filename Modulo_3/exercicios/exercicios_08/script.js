function contString(conta){
   
  // 
   if (conta.length > 10) {
      
      console.log(`testo muito grande tem ${conta.length} caracteres `);
      
   }else{
      console.log(`testo dentro do limite tem ${conta.length}  caracteres`);

   }



}

contString("ola");
contString("ola");
contString("ola tudo bem  com voce");
